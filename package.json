{
  "name": "myvault",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "个人收藏馆 — Vue 3 SPA，数据即主权",
  "scripts": {
    "data": "node scripts/build.js",
    "dev": "node scripts/build.js && vite",
    "build": "node scripts/build.js && vite build",
    "preview": "vite preview",
    "export": "node scripts/export-excel.js"
  },
  "dependencies": {
    "vue": "^3.5.13",
    "vue-router": "^4.5.0",
    "xlsx": "^0.18.5"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.2.4",
    "vite": "^6.3.5"
  },
  "engines": {
    "node": "20.x"
  }
}
